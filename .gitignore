target/
*.swp
*.swo
.DS_Store
*.db
__pycache__/
.artifacts/
# Gate run-frozen inputs, acquired from their checked-in pins and cached by
# the pin's own digest. Reproducible, so never committed.
.cache/

# The pinned v0.3.20 migration fixture is a checked-in gate input, not a
# build artifact, and `*.db` above would otherwise hide it.
!tests/fixtures/vz-0.4/migration/*.db
