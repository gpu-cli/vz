# Isolated release-gate validation tooling, not product runtime dependencies.
# Compile: uv pip compile --python /usr/bin/python3 --generate-hashes --no-emit-index-url scripts/helpers/gate-requirements.in --output-file scripts/helpers/gate-requirements.txt
jsonschema==4.23.0
